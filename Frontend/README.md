# Global Pulse Integrated Frontend (Node 16)

Flow: Landing → Login/Signup → Verification → OTP → Complete Profile → Welcome → Dashboard.

## Run

```bash
npm install
npm run dev
```

Dashboard routes start at `/dashboard`. The duplicate dashboard landing and login pages were removed.
